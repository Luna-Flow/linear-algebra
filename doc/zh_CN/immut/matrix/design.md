# immut/matrix 设计

## 职责

- 提供值语义导向的 `Matrix[T]`，转换操作返回新矩阵。
- 通过不可变向量支撑的表示保持按行优先存储。
- 对可能失败的矩阵操作使用带检查 API，让形状、指数和输入域错误通过 `Result[..., LinearAlgebraError]` 表达。

## 不变量

- 构造器会拒绝负维度和行长不一致的输入。
- 索引访问、行迭代器和列迭代器与可变矩阵 API 保持一致的边界语义。
- 当 `immut` 与 `mutable` 都暴露同一代数操作时，应保持语义对齐，同时保留不可变值语义。
