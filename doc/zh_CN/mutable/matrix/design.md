# mutable/matrix 设计

## 职责

- 提供执行导向的 `Matrix[T]`，在支持的后端上保持按行优先的 `Array[T]` 存储模型。
- 让行视图、列视图和转置视图始终连接到底层矩阵。
- 为运行时失败保留带检查 API，同时用显式 `unchecked_*` 方法承载旧契约。

## 不变量

- 后端专用文件可以优化内核，但必须暴露相同的公开语义。
- 矩阵、视图、迭代器和转置访问都执行显式边界检查。
- 当 `mutable` 与 `immut` 都暴露同一代数操作时，应保持语义对齐。
