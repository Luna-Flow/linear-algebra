# arithmetic 设计

## 职责

- 把低层可计算操作和语义代数律分开。
- 复用生态已有标量 traits，而不是在本仓库重复定义。
- 只在缺少线性代数集成名称时添加小的操作级 trait。

## 非职责

- 不定义向量空间、模、矩阵或后端能力。
- 不让浮点类型声称精确代数律。
- 不导入 `immut`、`mutable` 或 `backends/default`。

## 维护说明

新增 trait 时，名字应直接对应具体操作，并明确写出：它表示“这个操作可用”，而不是“这里成立一个数学结构”。
