///|
pub using @generic {trait Conjugate, trait Inverse, trait One, trait Zero}

///|
pub using @arith {
  type ArithmeticContext,
  type ArithmeticError,
  type ArithmeticErrorKind,
  type FpClass,
  type RoundingMode,
  trait Cbrt,
  trait CompareChecked,
  trait Constants,
  trait DivChecked,
  trait Exponential,
  trait Logarithmic,
  trait Power,
  trait Sqrt,
  trait SqrtChecked,
}
