///|
using @lg {
  trait One,
  trait Zero,
  trait Conjugate,
  trait Inverse,
  trait AddMonoid,
  trait MulMonoid,
  trait Semiring,
  trait Field,
  trait Num,
}

///|
using @qc_core {trait Arbitrary}
